<Window x:Class="GSharp.Builder.WindowPreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GSharp.Builder"
        mc:Ignorable="d"
        Title="모듈 미리 보기" Height="350" Width="500" WindowStartupLocation="CenterOwner">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        
        <ListBox Grid.Row="0" x:Name="ListBlocks" BorderBrush="{x:Null}" SnapsToDevicePixels="True" UseLayoutRounding="True"/>
        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="0,1,0,0">
            <ListBox x:Name="ListReferences" BorderBrush="{x:Null}"/>
        </Border>
    </Grid>
</Window>
